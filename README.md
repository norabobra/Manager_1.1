# MaFile Manager 🗂️

**MaFile Manager** --- это десктопное приложение на Python (PySide6) для
удобной обработки и управления `.maFile` JSON-файлами.

Приложение предоставляет графический интерфейс для пакетной обработки
файлов, организации данных и автоматизации работы с аккаунтами.

------------------------------------------------------------------------

## 🚀 Возможности

-   📂 Выбор папки с `.maFile` файлами
-   ⚙️ Пакетная обработка JSON-файлов
-   🧠 Автоматическое извлечение данных (например, `account_name`)
-   📁 Создание структурированных выходных директорий
-   📊 Отображение прогресса выполнения
-   📝 Логирование операций в реальном времени
-   🖥️ Удобный графический интерфейс (PySide6)

------------------------------------------------------------------------

## 🛠 Tech Stack

-   **Python 3.11+**
-   **PySide6** (Qt for Python)
-   Стандартные библиотеки Python (`os`, `json`, `shutil`)

------------------------------------------------------------------------

## 📦 Установка

### 1️⃣ Клонирование репозитория

``` bash
git clone https://github.com/yourusername/mafile-manager.git
cd mafile-manager
```

### 2️⃣ Установка зависимостей

Рекомендуется использовать виртуальное окружение:

``` bash
python -m venv venv
venv\Scripts\activate   # Windows
# source venv/bin/activate  # macOS/Linux

pip install -r requirements.txt
```

------------------------------------------------------------------------

## ▶️ Запуск приложения

### 🔹 Запуск из исходников

``` bash
python launcher.py
```

------------------------------------------------------------------------

## 🧱 Сборка (компиляция в .exe)

Для сборки проекта используется:

``` bash
build.bat
```

Скрипт предназначен для Windows и выполняет компиляцию приложения в
исполняемый файл.

------------------------------------------------------------------------

## 📂 Структура проекта

    project/
    │
    ├── launcher.py          # Точка входа для запуска приложения
    ├── build.bat            # Скрипт компиляции (Windows)
    ├── requirements.txt     # Зависимости
    │
    ├── core/                # Логика обработки файлов
    │   ├── processor.py
    │   ├── fs.py
    │   └── asf.py
    │
    ├── ui/                  # Интерфейс (PySide6)
    │   ├── main_window.py
    │   └── styles.py
    │
    └── assets/              # Иконки и ресурсы

------------------------------------------------------------------------

## 📄 Лицензия

Вы можете добавить подходящую лицензию (например, MIT).

------------------------------------------------------------------------

## 👤 Автор

Разработано для публичного использования.\
Если проект оказался полезным --- поставьте ⭐ на GitHub!
